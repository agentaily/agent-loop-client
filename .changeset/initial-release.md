---
"@agentaily/agent-loop-client": minor
---

Initial release: run the `@agentaily/agent-loop` agent loop in the browser — `createClientAgent` (DeepSeek direct, no server), `LocalSessionStore` / `LocalMemoryStore` (localStorage-backed, injectable `StorageLike`), and a re-export of the full `@agentaily/agent-loop` surface so consumers import one package.
